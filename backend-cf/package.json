{
  "name": "biz499-backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 execute biz499-db --local --file=./migrations/0001_initial.sql",
    "db:migrate:remote": "wrangler d1 execute biz499-db --remote --file=./migrations/0001_initial.sql",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "hono": "^4.4.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240620.0",
    "typescript": "^5.5.0",
    "wrangler": "^3.60.0"
  }
}
